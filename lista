from operator import add #importação de funções

def soma(x,y): #criar função soma
    return x + y #retorna valor de x e y
def sub(x,y):
    return x - y
def mult (x,y):
    return x * y

#teste soma
#assert soma(5,4) == 11 "Valor errado" #teste de soma ==11 com teste de erro
